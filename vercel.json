{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm ci --omit=dev",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "memory": 512,
      "maxDuration": 60
    }
  }
}
